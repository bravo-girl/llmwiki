{
  "name": "llmwiki",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "tsc --noEmit",
    "sources:complete-ingest": "python scripts/collect-complete-sources.py",
    "history:complete-ingest": "node scripts/record-complete-ingest-history.mjs",
    "test": "node --test"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260620.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.22.0"
  }
}
